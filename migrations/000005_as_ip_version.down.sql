-- Rollback: remove ip_version from AS tables (not practical, just documents the migration)
-- Manual intervention needed to restore original schema
SELECT 'Migration 000005 rollback not automated — restore from backup if needed';
